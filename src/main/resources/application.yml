server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5334/Driver
    username: mac62
    password: ~@292003@~
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet